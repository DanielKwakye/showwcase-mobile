import 'package:flutter/material.dart';

class UserSocialsWidget extends StatelessWidget {
  const UserSocialsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
