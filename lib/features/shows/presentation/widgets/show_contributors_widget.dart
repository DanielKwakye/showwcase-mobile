import 'package:flutter/material.dart';

class ShowContributorsWidget extends StatelessWidget {
  const ShowContributorsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }


}
