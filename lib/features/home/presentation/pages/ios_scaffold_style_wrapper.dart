import 'package:flutter/material.dart';

class IOSScaffoldStyleWrapper extends StatelessWidget {
  final Widget body;
  const IOSScaffoldStyleWrapper({Key? key, required this.body}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return body;
  }
}
