enum Appearance {
  primary, secondary, clean, error, success, dark
}

enum Position {
  top, bottom
}

enum AppButtonType {
  primary, secondary
}

enum VideoType { youtube, regular }
enum VideoSource { file, network, asset, mediaId }

